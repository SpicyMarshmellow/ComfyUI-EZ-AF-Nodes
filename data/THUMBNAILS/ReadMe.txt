For thumbnails to be applied correctly, their relative directory path and name should match prompt file 1 to 1 (except file extension)

To avoid long loading times, avoid using larger files as thumbnails, they are loaded in 80×80 size anyway